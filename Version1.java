
public class Version1 {
	public static void calculatePopulation(int[] bounds, CensusData data, float[] corner, int x, int y) {	
		double latUnit = (corner[1] - corner[0]) / x;
		double longUnit = (corner[3] - corner[2]) / y;
		
	}
}
